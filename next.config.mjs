/** @type {import('next').NextConfig} */
const nextConfig = {
  allowedDevOrigins: ['192.168.56.1', 'localhost', '10.2.0.2', '*.local'],
  // any other config options you might have
};

export default nextConfig;